#include<stdio.h>

int main(){
int i=0;
while (i<10)
{
i++;
if(i==6){
continue;
}  
printf("%d ",i);
}
return 0;
}

/*
บรรทัดที่ 4 ประกาศตัวแปร i = 0
บรรทัดที่ 5 ลูปจะทำงานต่อเมื่อ i<10 เป็นจริง
บรรทัดที่ 7 เพิ่มค่าของ i ขึ้นทีละ 1
บรรทัดที่ 8 ถ้า i เท่ากับ 6
บรรทัดที่ 9 ข้ามรอบนี้ไป
บรรทัดที่ 11 แสดงผลค่าของ i
บรรทัดที่ 13 จบการทำงานของโปรแกรม
*/