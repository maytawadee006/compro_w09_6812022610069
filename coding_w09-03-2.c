#include<stdio.h>

int main(){
int i=0;
do{  
i++;
if(i==6){
continue;
} 
printf("%d ",i);
}while (i<10);
return 0;
}

/*
บรรทัดที่ 4 ประกาศตัวแปร i = 0
บรรทัดที่ 6 ค่าของ i เพิ่มขึ้นทีละ 1
บรรทัดที่ 7 ถ้า i เท่ากับ 6
บรรทัดที่ 8 ข้ามรอบนี้ไป
บรรทัดที่ 10 แสดงผลลัพธ์ค่าของ I
บรรทัดที่ 11 ลูปจะทำงานต่อเมื่อ i<10 เป็นจริง
บรรทัดที่ 12 จบการทำงานของโปรแกรม
*/